const initialState = {
  name: 'sfve',
  description: 'svezv',
  photo: 'bvsvr',
  user: 'gfeavse'
}

const bid = (state = initialState, action) => {
  switch (action.type) {
    case 'test':
      return state
    default:
      return state
  }
}

export default bid
